<Window x:Class="TrajectoryOfSensorVisualization.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxycontrols="http://oxyplot.org/wpf/contrib"
        xmlns:local="clr-namespace:TrajectoryOfSensorVisualization.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <oxycontrols:Plot x:Name="PlotXY" Model="{Binding PlotModelXYPlane}" Grid.Column="0" Grid.Row="0"></oxycontrols:Plot>
            <oxycontrols:Plot x:Name="PlotXZ" Model="{Binding PlotModelXZPlane}" Grid.Column="0" Grid.Row="2"></oxycontrols:Plot>
            <oxycontrols:Plot x:Name="PlotYZ" Model="{Binding PlotModelYZPlane}" Grid.Column="0" Grid.Row="4"></oxycontrols:Plot>
        </Grid>
    </Grid>
</Window>
